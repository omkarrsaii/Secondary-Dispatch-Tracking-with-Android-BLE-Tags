# Skip Playwright's automatic browser download.
# This app uses real installed Chrome instead of Playwright's bundled Chromium.
# This also avoids SSL certificate errors on corporate networks.
PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
